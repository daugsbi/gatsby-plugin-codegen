{
  "name": "gatsby-plugin-codegen",
  "repository": {
    "type": "git",
    "url": "https://github.com/daugsbi/gatsby-plugin-codegen"
  },
  "version": "1.0.7",
  "description": "Wrapper of apollo:codegen to generate client side types from your queries as well as the gatsby schema & apollo config to provide autocomplete with apollographql.vscode-apollo",
  "keywords": [
    "gatsby",
    "gatsby-plugin",
    "gatsby-plugin-generate-types",
    "apollo",
    "generate-types",
    "autocomplete"
  ],
  "main": "index.js",
  "scripts": {
    "build": "tsc -p ./",
    "watch": "tsc -p ./ -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Denis Augsburger <denis.augsburger@twteam.ch> (https://twteam.ch)",
  "license": "MIT",
  "dependencies": {
    "apollo": "^2.21.0",
    "graphql": "^14.5.8",
    "lodash.mergewith": "^4.6.2"
  },
  "devDependencies": {
    "@types/lodash.merge": "^4.6.6",
    "gatsby": "^2.15.15",
    "typescript": "^3.6.3"
  },
  "peerDependencies": {
    "gatsby": "^2.0.0"
  },
  "engines": {
    "node": ">=8.0.0"
  }
}
